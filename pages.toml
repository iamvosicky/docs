# Configuration for Cloudflare Pages

[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[site]
  bucket = "./.next"
  entry-point = "_worker.js"

[env.production]
  NODE_ENV = "production"

[env.preview]
  NODE_ENV = "production"
